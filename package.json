{
  "name": "lokalmatador-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "AWS_PROFILE=private sst dev",
    "deploy:dev": "AWS_PROFILE=private sst deploy --stage dev",
    "deploy": "AWS_PROFILE=private sst deploy --stage production",
    "remove:dev": "AWS_PROFILE=private sst remove --stage dev",
    "remove": "AWS_PROFILE=private sst remove --stage production"
  },
  "devDependencies": {
    "sst": "4.6.9",
    "typescript": "^5.0.0"
  }
}
